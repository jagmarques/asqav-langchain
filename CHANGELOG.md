# Changelog

All notable changes to `asqav-langchain` are listed here.

Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Versions follow [SemVer](https://semver.org/) and track the PyPI release.

## [Unreleased]

## [0.1.0]

Initial release. LangChain and LangGraph `BaseCallbackHandler` that signs `tool:start`, `tool:end`, and `tool:error` events on every agent tool call. Fail-open signing through the Asqav API.
